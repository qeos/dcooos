void _main(){
    // *********************************************************************
    // INITIALIZATION
    // *********************************************************************

    int *scr = 0xb800;

    char *s = "Hello world from kernel under x64";

    do{
        *scr++ = *s++;
    }while(*s != 0);

    while(1==1);

}
